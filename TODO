## TODO

* omq-keygen is gone now. add its functionality to the main CLI. will require libsodium. is it safe to generate keys with Nuckle?
* io-endpoint integration: OMQ-aware endpoints, on Zeroids
  - Sam Williams says: AddressEndpoint and CompositeEndpoint already solve some of those issues.
* memory leak tests for ipc and tcp
* snap package: both gems, ruby interpreter and libsodium included = one powerful cli tool to rule it all
* pure Ruby NNG just like this gem


## Future ideas
- OMG: OMQ for NNG
